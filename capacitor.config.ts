import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.app.lars3',
  appName: 'blueprint',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
